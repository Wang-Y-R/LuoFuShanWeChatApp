<view class="tabbar">
  <block wx:for="{{list}}" wx:key="pagePath">
    <view class="tab-item {{selected==index?'active':''}}" data-index="{{index}}" bindtap="onSwitch">
      <view class="icon">{{item.icon}}</view>
      <view class="label">{{item.text}}</view>
    </view>
  </block>
</view>
