<view class="container">
  <view class="content">
    <image src="{{detail.cover}}" mode="aspectFill" style="width:100%;height:320rpx" />
    <view class="section-title">{{detail.name}}</view>
    <view style="color:#666;">营业时间 {{detail.hours}}</view>
    <view wx:for="{{detail.sections}}" wx:key="index">
      <view wx:if="{{item.type==='text'}}" style="margin-top:16rpx;line-height:1.6;color:#333;">{{item.value}}</view>
      <image wx:elif="{{item.type==='image'}}" src="{{item.value}}" mode="widthFix" style="width:100%;margin-top:16rpx;border-radius:12rpx" />
    </view>
  </view>
  <!-- 固定在底部的导航按钮 -->
  <view class="bottom-nav" bindtap="onNavigate">
    <text class="nav-icon">🗺️</text>
    <text class="nav-text">导航</text>
  </view>
</view>
